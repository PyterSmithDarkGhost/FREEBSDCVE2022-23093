Gerando o ataque com um shellcode Stack Based buffer overflow associando algum privilégio semelhante que se encaixa neste cve, ou seja pingando com execução remota de código com um simples shellcode vc terá acesso root no servidor do FreeBSD sem mesmo quaisquer outra agressão pra liberar acesso

Ferramentas essênciais para a exploração deste CVE: motor pra gerar o shellcode, wireshark, burp suite, armitage, metasploit, searchsploit.
